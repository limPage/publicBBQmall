if(bid.value==='pi'){
    window.document.getElementById('MSideBarPi').style.cssText = `  color: rgba(0,0,0,0.9);
    font-weight: 700;`;
}
if(bid.value==='pr'){
    window.document.getElementById('MSideBarPr').style.cssText = `  color: rgba(0,0,0,0.9);
    font-weight: 700;`;
}
if(bid.value==='bp'){
    window.document.getElementById('MSideBarBp').style.cssText = `  color: rgba(0,0,0,0.9);
    font-weight: 700;`;
}
